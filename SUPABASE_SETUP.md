# Инициализация базы данных Supabase

## Шаг 1: Выполнение миграции

### Вариант A: Использование Supabase CLI (рекомендуется)

```bash
supabase db push
```

### Вариант B: Вручную через Supabase Dashboard

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **SQL Editor**
4. Нажмите **"New query"**
5. Скопируйте содержимое файла `supabase/migrations/init_database.sql`
6. Вставьте в редактор
7. Нажмите **"Run"**

---

## Шаг 2: Создание тестовых данных (опционально)

Выполните этот SQL запрос для создания тестовой эпохи и урока:

```sql
-- Добавить тестовую эпоху
INSERT INTO eras (title, description, order_index)
VALUES ('Древний мир', 'История древних цивилизаций', 1);

-- Добавить тестовую битву на карту
INSERT INTO historical_battles (title, description, year, latitude, longitude)
VALUES ('Битва при Марафоне', 'Древняя битва между греками и персами', 490, 38.1405, 24.0298);

-- Добавить тестовое достижение
INSERT INTO achievements (title, description, icon, xp_reward)
VALUES ('Первый шаг', 'Пройти первый урок', '🎓', 10);
```

---

## Шаг 3: Создание профиля для пользователя

Когда пользователь входит в приложение, его профиль нужно создать вручную (пока автоматического создания нет):

```sql
-- Вставьте UUID пользователя (см. в таблице auth.users)
INSERT INTO user_profiles (id, level, xp, streak)
VALUES ('ВАШЕ_USER_ID_ЗДЕСЬ', 1, 0, 0);
```

Где `ВАШЕ_USER_ID_ЗДЕСЬ` - это UUID из таблицы `auth.users` для вашего пользователя.

---

## Структура таблиц

### eras (Эпохи)
- `id` (UUID) - Primary Key
- `title` (TEXT) - Название эпохи
- `description` (TEXT) - Описание
- `order_index` (INTEGER) - Порядок отображения

### lessons (Уроки)
- `id` (UUID) - Primary Key
- `title` (TEXT) - Название урока
- `content` (TEXT) - Содержание урока (markdown/html)
- `era_id` (UUID FK) - Ссылка на эпоху
- `order_index` (INTEGER) - Порядок в эпохе

### questions (Вопросы викторины)
- `id` (UUID) - Primary Key
- `lesson_id` (UUID FK) - Ссылка на урок
- `question_text` (TEXT) - Текст вопроса
- `options` (TEXT[]) - Массив вариантов ответа
- `correct_answer` (INTEGER) - Индекс правильного ответа (0-3)
- `order_index` (INTEGER) - Порядок вопроса

### user_profiles (Профили пользователей)
- `id` (UUID FK) - User ID из auth.users
- `level` (INTEGER) - Уровень пользователя
- `xp` (INTEGER) - Опыт (Experience Points)
- `streak` (INTEGER) - Дневная активная серия
- `last_activity_date` (DATE) - Последний день активности

### achievements (Достижения)
- `id` (UUID) - Primary Key
- `title` (TEXT) - Название достижения
- `description` (TEXT) - Описание
- `icon` (TEXT) - Emoji или URL иконки
- `xp_reward` (INTEGER) - Награда за достижение

### user_achievements (Достижения пользователя)
- `id` (UUID) - Primary Key
- `user_id` (UUID FK) - ID пользователя
- `achievement_id` (UUID FK) - ID достижения
- `unlocked_at` (TIMESTAMP) - Когда разблокировано

### user_progress (Прогресс пользователя)
- `id` (UUID) - Primary Key
- `user_id` (UUID FK) - ID пользователя
- `lesson_id` (UUID FK) - ID урока
- `completed` (BOOLEAN) - Завершён ли урок
- `score` (INTEGER) - Количество правильных ответов
- `completed_at` (TIMESTAMP) - Когда завершён

### historical_battles (Исторические битвы)
- `id` (UUID) - Primary Key
- `title` (TEXT) - Название битвы
- `description` (TEXT) - Описание
- `year` (INTEGER) - Год события
- `latitude` (DECIMAL) - Широта
- `longitude` (DECIMAL) - Долгота
- `video_url` (TEXT) - URL видео (YouTube и т.д.)

---

## Включение RLS (Row Level Security) - будущее

Когда будете готовы, создайте RLS политики для безопасности:

```sql
-- Включить RLS для user_profiles
ALTER TABLE user_profiles ENABLE ROW LEVEL SECURITY;

-- Политика: пользователи видят только свой профиль
CREATE POLICY "Users can view their own profile"
  ON user_profiles FOR SELECT
  USING (auth.uid() = id);

-- Политика: пользователи могут обновлять только свой профиль
CREATE POLICY "Users can update their own profile"
  ON user_profiles FOR UPDATE
  USING (auth.uid() = id);
```

---

## Решение проблем

### Ошибка "406 PGRST116"
Это значит запись не найдена в таблице. Решение:
1. Убедитесь что таблица `user_profiles` существует
2. Создайте профиль для пользователя используя SQL выше

### Ошибка "Relation does not exist"
Значит таблица не создана. Выполните SQL скрипт инициализации.

---

## Встроенные функции

### add_xp(user_id, xp, reason)
```sql
SELECT add_xp('user-uuid-here'::uuid, 50, 'Викторина пройдена');
```
