# ❓ Почему профиль не находится? (PGRST116 ошибка)

## Проблема

Вы видите ошибку **406 PGRST116** при попытке загрузить профиль:

```
GET https://opnqqailwtqezvqsodfv.supabase.co/rest/v1/user_profiles?select=*&id=eq.721f041b...
Status: 406
Error: PGRST116
```

Но ID пользователя `721f041b-68c6-427f-818f-be835b342ef2` существует в `auth.users` ✅

---

## Причина

**PostgREST API возвращает 406, когда запрос ожидает одну строку (`.single()`), но результат пуст!**

В вашем коде:

```typescript
const { data: profileData, error: profileError } = await supabase
  .from('user_profiles')
  .select('*')
  .eq('id', userId)
  .single()  // ← Ожидает 1 результат, но находит 0
```

**Таблица `user_profiles` пуста для этого пользователя!**

---

## Решение

### Вариант 1: Создать профиль вручную

**Выполните в Supabase Dashboard → SQL Editor:**

```sql
INSERT INTO user_profiles (id, level, xp, streak)
VALUES ('721f041b-68c6-427f-818f-be835b342ef2', 1, 0, 0);
```

### Вариант 2: Создать для всех пользователей без профиля

```sql
INSERT INTO user_profiles (id, level, xp, streak)
SELECT id, 1, 0, 0
FROM auth.users
WHERE id NOT IN (SELECT id FROM user_profiles);
```

### Вариант 3: Автоматическое создание (рекомендуется)

Если миграция выполнена, триггер `on_auth_user_created` **автоматически создаст профиль** при регистрации нового пользователя.

**Для миграции:**
```bash
supabase db push
```

---

## Проверка

После создания профиля:

1. Откройте браузер консоль (F12)
2. Перейдите на `/profiles/721f041b-68c6-427f-818f-be835b342ef2`
3. Должны увидеть профиль или сообщение об ошибке загрузки (но не 406)

---

## Почему это произошло?

🔴 **Что случилось:**
1. Вы зарегистрировались → пользователь добавлен в `auth.users`
2. Но запись в `user_profiles` не была создана (триггер не был включен)
3. Приложение ищет профиль и не находит → ошибка 406

🟢 **Как этого избежать:**
1. ✅ Выполнить полную миграцию (`supabase db push`)
2. ✅ Новые пользователи будут автоматически получать профиль
3. ✅ Для существующих - создать профили вручную или батчем

---

## SQL триггер (что происходит автоматически)

```sql
-- Функция создания профиля
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS trigger AS $$
BEGIN
  INSERT INTO public.user_profiles (id, level, xp, streak)
  VALUES (new.id, 1, 0, 0);
  RETURN new;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

-- Триггер на вставку нового пользователя
CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION public.handle_new_user();
```

Когда пользователь регистрируется:
1. Суpabase добавляет его в `auth.users`
2. Триггер автоматически создаёт запись в `user_profiles`
3. Профиль готов к использованию

---

## 📝 Файл для создания профилей

Смотрите `supabase/create_profile.sql` для готовых SQL запросов.

---

## Дополнительно

- Если RLS будут включены, может потребоваться `SECURITY DEFINER` для триггера
- Текущая версия RLS отключена, поэтому триггер работает без ограничений
- Для production рекомендуется включить RLS с правильными политиками
